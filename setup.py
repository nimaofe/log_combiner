from setuptools import setup, find_packages

setup(
    name='log_combiner',
    version='0.1',
    packages=find_packages(),
    description='A tool to combine and view MySQL and IIS logs on a Windows server',
    author='Your Name',
    author_email='your.email@example.com',
    install_requires=[
        # Add any dependencies here
    ],
)
